# Astroleet currently requires no runtime environment variables.
#
# Add future provider credentials only when their server-side integration ships.
# Never commit real values and never prefix secrets with NEXT_PUBLIC_.
